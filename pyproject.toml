[project]
name = "defragger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotbimpy>=0.1.0",
    "ifcopenshell>=0.8.4.post1",
]
